PROBAR CLASE RELOJ:
	modificar hora
	modificar fecha
	modificar hora fecha
	modificar fecha hora
	comprobar que solo se pueda adelantar o dejar igual que hora actual
	si el nueva_reloj es < antiguo_reloj -> ERROR

INSERTAR TAREA
	comprobar que si se añade sin fecha se pone la fecha_actual
	si se añade una tareas en el pasado -> ERROR
	si concide una tarea del futuro -> ERROR
	comprobar que se pueda añadir sin etiqueta
	comprobar que se puedan añadir mas de 1 etiqueta

CONSULTA TAREA
	comprobar que no se pueda consultar sobre tareas del pasado
	comprobar que solo se pueda hacer consulta en un intervalo en tareas del futuro
	comprobar que en caso que buscar_fecha_superior < buscar_fecha_inferior no se imprime nada
	comprobar que en caso buscar_fecha_superior & buscar_fecha_inferior sean de l passado no se escriba nada
	comprobar que en caso buscar_fecha_inferior sea del pasado y buscar_fecha_superior sea del futuro solo se escribe desde fecha actual hasta buscar_fecha_superior
	comprobar que las consultas por intervalo comienzas la busqueda desde 00:00 fecha incial hasta 23:59 fecha final
	comprobar que se pueda realizar busqueda en un intervalo con solo 1 fecha
	Menu:
	-comprobar que las etiquetas del menu esten ordenadas de forma lexicografico
	Exprsiones:
	-comprobar las busquedas con expresiones
	-comprobar busqueda con solo 1 etiqueta
	-comprobar busqueda sin etiquetas
	comrpobar qeu se pueda buscar solo por expresiones sin fechas
	comprobar que las buscaquedas sin resultados no impriman nada


MODIFICAR TAREA
	comrobar que solo se pueda modificar tareas del futuro
	comprobar modificaciones todo tipo(fecha,titulo,hora,etiquetas)
	si fecha y hora son anteriores reloj_Actual -> ERROR
	si existe una tarea en la nueva fecha y hora -> ERROR
	si en el menu no esta ese numero a cambiar -> ERROR
	si la tarea a modificar se ha borrado -> ERROR
	si la tarea a modificar es del pasado -> ERROR
	comprobar varias combinaciones de modificaciones

BORRAR TAREA
	comrpobar que se puda borrar la tarrea con '-k'
	comrpobar que con 'k -#[etiqueta]' se bora solamente esa etiqueta
	comrobar que con 'k -etiquetes' se borran todas las etiquetas
	si la etiqueta no pertenece a la tarea - > ERROR
	si el numero de etiqueta no esta en el menu -> ERROR
	si la etiqueta ya se ha borrado -> ERROR
	si la etiqueta es del pasado -> ERROR
	
